class TileNode : ShapedNode {
	private id: string;
	private data: number;
	
	TileNode(gravity: boolean = true, renderable: boolean = true, id: string, data: number) {
		super(true, false, gravity, renderable, id, data);
		boxes[0] = ;
	}
}
